[package]
name = "diffplayerqc-core"
version = "0.1.0"
edition = "2021"
description = "Lógica de dominio DiffPlayerQC v2 (sin egui ni wgpu)"
license = "MIT"

[lib]
name = "diffplayerqc_core"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
